DATABASE_URL="file:./dev.db"
AUTH_SECRET="your-secret-key-min-32-characters-long"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
# Unsplash API — photo search in carousel generator (free: unsplash.com/developers)
UNSPLASH_ACCESS_KEY=""
# Google Places API — real restaurant search + Google Maps photos (free tier: console.cloud.google.com)
GOOGLE_PLACES_API_KEY=""
# Sécurise les webhooks /api/webhook/* — si défini, ManyChat/n8n doivent
# l'envoyer via le header x-webhook-secret ou ?secret= (optionnel)
WEBHOOK_SECRET=""
